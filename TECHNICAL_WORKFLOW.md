# 🔄 SilentVoice AI - Technical Workflow Documentation

## 📋 Complete System Workflow

This document explains the complete technical workflow of the SilentVoice AI application, from app launch to every feature interaction.

---

## 🚀 App Launch Workflow

### **Step-by-Step Process:**

```
┌─────────────────────────────────────────────┐
│ 1. User Taps App Icon                       │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. main.dart - Entry Point                  │
│    - Initialize Flutter Framework           │
│    - Create ProviderScope (Riverpod)        │
│    - Configure GoRouter                     │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Splash Screen Displays                   │
│    - Show App Logo & Name                   │
│    - Play Fade-in Animation                 │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Background Initialization (Parallel)     │
│    ├─ Load TFLite Model (TFLiteService)     │
│    ├─ Initialize SQLite DB (DatabaseService)│
│    ├─ Load Settings (SharedPreferences)     │
│    └─ Request Permissions                   │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 5. Check Initialization Status              │
│    - Model Loaded? ✅                        │
│    - Database Ready? ✅                      │
│    - Settings Loaded? ✅                     │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 6. Navigate to Home Screen                  │
│    - After 3 seconds delay                  │
│    - Or immediately if all loaded           │
└─────────────────────────────────────────────┘
```

---

## 🎥 Camera Detection Workflow (Most Important!)

### **Complete Detection Pipeline:**

```
User Opens Camera Screen
         |
         v
┌─────────────────────────────────────────────┐
│ INITIALIZATION PHASE                         │
├─────────────────────────────────────────────┤
│ 1. Check Camera Permission                  │
│    - If DENIED → Request Permission         │
│    - If GRANTED → Continue                  │
│    - If PERMANENTLY DENIED → Show Settings  │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Initialize Camera Controller             │
│    - Select back camera                     │
│    - Set resolution: ResolutionPreset.medium│
│    - Configure for iOS/Android              │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Load TFLite Model (if not loaded)        │
│    - Read from assets/models/               │
│    - Create Interpreter                     │
│    - Allocate tensors                       │
│    - Verify input/output shapes             │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Start Camera Preview                     │
│    - Display live feed on screen            │
│    - Resolution: 640x480 (default)          │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ DETECTION LOOP (Continuous)                 │
└─────────────────────────────────────────────┘
         |
         v
┌─────────────────────────────────────────────┐
│ 5. Start Image Stream                       │
│    - Capture frames at ~10 FPS              │
│    - camera.startImageStream()              │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ For Each Frame      │
         └──────────┬──────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. IMAGE PREPROCESSING                      │
├─────────────────────────────────────────────┤
│ A. Convert CameraImage to Image             │
│    - Format: YUV420 → RGB                   │
│    - Original size: 640x480                 │
│                                             │
│ B. Resize Image                             │
│    - From: 640x480                          │
│    - To: 224x224                            │
│    - Method: Bilinear interpolation         │
│                                             │
│ C. Normalize Pixels                         │
│    - From range: 0-255                      │
│    - To range: 0.0-1.0                      │
│    - Formula: pixel / 255.0                 │
│                                             │
│ D. Convert to Float32List                   │
│    - Shape: [1, 224, 224, 3]                │
│    - Total elements: 150,528 floats         │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 7. MODEL INFERENCE                          │
├─────────────────────────────────────────────┤
│ A. Prepare Input Tensor                     │
│    input = [1, 224, 224, 3]                 │
│                                             │
│ B. Run Interpreter                          │
│    interpreter.run(input, output);          │
│    - Duration: ~50-100ms                    │
│                                             │
│ C. Get Output Tensor                        │
│    output = [1, 107]                        │
│    - 107 probability values                 │
│    - Each represents one gesture class      │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 8. POST-PROCESSING                          │
├─────────────────────────────────────────────┤
│ A. Find Maximum Probability                 │
│    maxIndex = 0                             │
│    maxValue = output[0]                     │
│    for i in 0..106:                         │
│      if output[i] > maxValue:               │
│        maxValue = output[i]                 │
│        maxIndex = i                         │
│                                             │
│ B. Check Confidence Threshold               │
│    if maxValue >= threshold (default 0.5):  │
│      ✅ Accept prediction                    │
│    else:                                    │
│      ❌ Reject (show "Low Confidence")       │
│                                             │
│ C. Map Index to Gesture Label               │
│    label = GestureLabels.allGestures[index] │
│    Example: index=0 → "A"                   │
│             index=25 → "Z"                  │
│             index=26 → "0"                  │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 9. TEMPORAL SMOOTHING (Optional)            │
│    - Keep last N predictions                │
│    - Use majority vote                      │
│    - Reduces jitter/flickering              │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 10. UPDATE UI                               │
├─────────────────────────────────────────────┤
│ A. PredictionOverlay                        │
│    - Show gesture label (e.g., "A")         │
│    - Display confidence (e.g., "87%")       │
│    - Animate change (fade in/out)           │
│                                             │
│ B. DetectedTextDisplay                      │
│    - Append gesture to text string          │
│    - Example: "" → "H" → "HE" → "HELLO"     │
│                                             │
│ C. ConfidenceMeter                          │
│    - Visual bar showing confidence          │
│    - Color: Green (high) to Red (low)       │
│                                             │
│ D. Trigger Effects (if enabled in settings) │
│    - Play sound (assets/sounds/detection.mp3)│
│    - Vibrate device (100ms)                 │
│    - TTS speak gesture                      │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ Loop Back to Step 6 │
         │ (Next Frame)        │
         └─────────────────────┘
```

---

### **User Actions During Detection:**

```
┌─────────────────────────────────────────────┐
│ USER PRESSES: "Add Space" Button            │
└──────────────────┬──────────────────────────┘
                   ↓
               Insert " " into detected text
               Example: "HELLO" → "HELLO "
                   ↓
               Continue detection
```

```
┌─────────────────────────────────────────────┐
│ USER PRESSES: "Delete" Button               │
└──────────────────┬──────────────────────────┘
                   ↓
         Remove last character from text
         Example: "HELLO" → "HELL"
                   ↓
               Continue detection
```

```
┌─────────────────────────────────────────────┐
│ USER PRESSES: "Clear All" Button            │
└──────────────────┬──────────────────────────┘
                   ↓
              Reset detected text to ""
                   ↓
               Continue detection
```

```
┌─────────────────────────────────────────────┐
│ USER PRESSES: "Save" Button                 │
└──────────────────┬──────────────────────────┘
                   ↓
         Create GestureHistory object
         {
           detectedText: "HELLO WORLD",
           timestamp: "2025-12-17 01:22:55",
           confidence: 0.87,
           duration: 45 // seconds
         }
                   ↓
         DatabaseService.insertHistory(history)
                   ↓
         Show success message: "✅ Saved to History"
                   ↓
               Return to Home Screen
```

```
┌─────────────────────────────────────────────┐
│ USER PRESSES: "Speak" Button                │
└──────────────────┬──────────────────────────┘
                   ↓
         TTSService.speak(detectedText)
         - Convert text to speech
         - Use system TTS engine
         - Language: English (default)
                   ↓
         Audio plays through speaker
```

---

## 📚 Learning Mode Workflow

```
User Opens Learning Screen
         |
         v
┌─────────────────────────────────────────────┐
│ 1. Load All Gestures                        │
│    - From GestureLabels.allGestures (107)   │
│    - Categories: Alphabet, Numbers,         │
│                  Words, Phrases             │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Load Learning Progress from Database     │
│    - Query: SELECT * FROM learning_progress │
│    - Get which gestures are marked learned  │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Display Gestures in Grid                 │
│    - 2 columns layout                       │
│    - For each gesture:                      │
│      [Image] [Name] [Checkbox]              │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Calculate and Show Progress              │
│    - Total learned / Total gestures * 100   │
│    - Example: 25/107 = 23%                  │
│    - Show in progress bar at top            │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ User Interaction    │
         └──────────┬──────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ USER TAPS: Checkbox                         │
└──────────────────┬──────────────────────────┘
                   ↓
         Toggle isLearned state
         (unchecked → checked OR checked → unchecked)
                   ↓
┌─────────────────────────────────────────────┐
│ 5. Update Database                          │
│    DatabaseService.markGestureAsLearned(    │
│      gestureId: "A",                        │
│      isLearned: true                        │
│    )                                        │
│                                             │
│    SQL: UPDATE learning_progress            │
│         SET is_learned = 1                  │
│         WHERE gesture_id = 'A'              │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 6. Recalculate Progress                     │
│    - New count of learned gestures          │
│    - Update progress bar                    │
│    - Animate change                         │
└──────────────────┬──────────────────────────┘
                   ↓
         UI Updates Immediately (Riverpod)
```

---

## 💪 Practice Mode Workflow

```
User Opens Practice Screen
         |
         v
┌─────────────────────────────────────────────┐
│ 1. Query Learned Gestures from Database     │
│    - SELECT * FROM learning_progress        │
│      WHERE is_learned = 1                   │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Check if Any Gestures Learned            │
│    - If count = 0:                          │
│      Show "Learn some gestures first!"      │
│      Return to Learning Screen              │
│    - If count > 0:                          │
│      Continue to practice                   │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Initialize Practice Session              │
│    - Total questions: 10 (default)          │
│    - Correct answers: 0                     │
│    - Current question: 1                    │
│    - Start timer                            │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Generate Random Question                 │
│    - Pick random gesture from learned list  │
│    - Example: expectedGesture = "H"         │
│    - Display: "Show the sign for: H"        │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 5. User Performs Gesture                    │
│    - Opens camera                           │
│    - Same detection pipeline as Camera Screen│
│    - Real-time feedback shown               │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 6. Capture User's Gesture                   │
│    - Wait for stable detection (2 sec)      │
│    - detectedGesture = model output         │
│    - Example: detectedGesture = "H"         │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 7. Validate Answer                          │
│    if (detectedGesture == expectedGesture): │
│      ✅ CORRECT                              │
│      - correctAnswers++                     │
│      - Show green checkmark                 │
│      - Play success sound                   │
│    else:                                    │
│      ❌ WRONG                                │
│      - Show red X                           │
│      - Show correct answer image            │
│      - Play error sound                     │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 8. Move to Next Question                    │
│    - currentQuestion++                      │
│    - If currentQuestion <= 10:              │
│      Go back to Step 4                      │
│    - If currentQuestion > 10:               │
│      End session → Show Results             │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 9. Show Results Screen                      │
│    - Score: 8/10 (80%)                      │
│    - Correct: 8 ✅                           │
│    - Wrong: 2 ❌                             │
│    - Time taken: 3:45                       │
│    - Review answers (optional)              │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 10. Save Practice Session to Database       │
│     (Optional feature)                      │
│     INSERT INTO practice_sessions...        │
└──────────────────┬──────────────────────────┘
                   ↓
         Return to Home Screen
```

---

## 🔄 Reverse Mode Workflow

```
User Opens Reverse Mode Screen
         |
         v
┌─────────────────────────────────────────────┐
│ 1. Show Text Input Field                    │
│    - Placeholder: "Enter text to translate" │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. User Types Text                          │
│    - Example: "HELLO"                       │
│    - Convert to uppercase automatically     │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Parse Input Text                         │
│    - Split into characters                  │
│    - inputChars = ['H', 'E', 'L', 'L', 'O'] │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Map Each Character to Gesture Image      │
│    for each char in inputChars:             │
│      gestureImage = getImageForChar(char)   │
│                                             │
│    Example mapping:                         │
│    'H' → assets/gestures/H.png              │
│    'E' → assets/gestures/E.png              │
│    'L' → assets/gestures/L.png              │
│    'L' → assets/gestures/L.png              │
│    'O' → assets/gestures/O.png              │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 5. Display Mode Selection                   │
│    ┌─────────┬────────────┐                │
│    │  Grid   │  Slideshow │                │
│    └─────────┴────────────┘                │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────┬─────────────────┐
         ↓                 ↓                 ↓
    GRID MODE         SLIDESHOW MODE         
         |                 |
         v                 v
┌────────────────┐  ┌────────────────────────┐
│ 6a. Grid View  │  │ 6b. Slideshow View     │
│ ┌──┬──┬──┐     │  │ Current: [H]           │
│ │H │E │L │     │  │         Large          │
│ ├──┼──┼──┤     │  │         Image          │
│ │L │O │  │     │  │                        │
│ └──┴──┴──┘     │  │ [< Prev] [Pause] [Next >]│
│                │  │ Speed: ● Normal        │
│ All visible    │  │ Timer: 2s per gesture  │
│ at once        │  │                        │
│                │  │ Auto-advance enabled   │
└────────────────┘  └────────────┬───────────┘
                                 ↓
                    ┌────────────────────────┐
                    │ Slideshow Logic:       │
                    │ 1. Show gesture 1 (H)  │
                    │ 2. Wait 2 seconds      │
                    │ 3. Fade transition     │
                    │ 4. Show gesture 2 (E)  │
                    │ 5. Repeat until end    │
                    │ 6. Loop if enabled     │
                    └────────────────────────┘
```

### **Reverse Mode Controls:**

```
Speed Control:
├─ Slow: 3 seconds per gesture
├─ Normal: 2 seconds per gesture
└─ Fast: 1 second per gesture

Play/Pause:
├─ Play: Auto-advance through gestures
└─ Pause: Stop on current gesture

Navigation:
├─ Previous: Go to previous gesture
├─ Next: Go to next gesture
└─ Jump to Index: Click any gesture in grid
```

---

## 📜 History Screen Workflow

```
User Opens History Screen
         |
         v
┌─────────────────────────────────────────────┐
│ 1. Query Database                           │
│    SELECT * FROM history                    │
│    ORDER BY timestamp DESC                  │
│    LIMIT 50                                 │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Parse Results                            │
│    - Convert SQL rows to GestureHistory     │
│    - Format timestamps                      │
│    - Calculate durations                    │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Check if Empty                           │
│    - If count = 0:                          │
│      Show empty state illustration          │
│      "No history yet. Start detecting!"     │
│    - If count > 0:                          │
│      Display list                           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Display History Items                    │
│    For each item:                           │
│    ┌──────────────────────────────────┐    │
│    │ 📅 Dec 17, 2025 - 1:22 AM        │    │
│    │ 💬 "HELLO WORLD"                 │    │
│    │ 📊 Confidence: 87%               │    │
│    │ ⏱️ Duration: 45s                  │    │
│    │ [View] [Delete]                  │    │
│    └──────────────────────────────────┘    │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ User Actions        │
         └──────────┬──────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ USER ACTION: Search                         │
├─────────────────────────────────────────────┤
│ 1. User types in search box                 │
│    SearchQuery: "HELLO"                     │
│                                             │
│ 2. Filter results                           │
│    WHERE detected_text LIKE '%HELLO%'       │
│                                             │
│ 3. Update ListView with filtered items      │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ USER ACTION: Delete Item                    │
├─────────────────────────────────────────────┤
│ 1. User clicks Delete button                │
│                                             │
│ 2. Show confirmation dialog                 │
│    "Are you sure you want to delete?"       │
│    [Cancel] [Delete]                        │
│                                             │
│ 3. If confirmed:                            │
│    DatabaseService.deleteHistory(itemId)    │
│                                             │
│ 4. SQL: DELETE FROM history                 │
│         WHERE id = ${itemId}                │
│                                             │
│ 5. Remove from ListView                     │
│    - Animate removal                        │
│    - Show snackbar: "Deleted successfully"  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ USER ACTION: Clear All History              │
├─────────────────────────────────────────────┤
│ 1. User clicks "Clear All" button           │
│                                             │
│ 2. Show warning dialog                      │
│    "This will delete ALL history!"          │
│    [Cancel] [Clear All]                     │
│                                             │
│ 3. If confirmed:                            │
│    DatabaseService.clearAllHistory()        │
│                                             │
│ 4. SQL: DELETE FROM history                 │
│                                             │
│ 5. Update UI to show empty state            │
└─────────────────────────────────────────────┘
```

---

## ⚙️ Settings Workflow

```
User Opens Settings Screen
         |
         v
┌─────────────────────────────────────────────┐
│ 1. Load Current Settings                    │
│    - SharedPreferences.getInstance()        │
│    - Read all saved preferences             │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Display Settings UI                      │
│    ┌──────────────────────────────────┐    │
│    │ DETECTION SETTINGS                │    │
│    │ Confidence: [======○===] 0.7      │    │
│    │ Speed: ● Normal                   │    │
│    ├──────────────────────────────────┤    │
│    │ AUDIO SETTINGS                    │    │
│    │ Sound Effects: ⚪ ON              │    │
│    │ Text-to-Speech: ⚪ ON             │    │
│    ├──────────────────────────────────┤    │
│    │ FEEDBACK                          │    │
│    │ Vibration: ⚪ ON                  │    │
│    ├──────────────────────────────────┤    │
│    │ DATA MANAGEMENT                   │    │
│    │ [Clear History]                   │    │
│    │ [Export Data]                     │    │
│    └──────────────────────────────────┘    │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ User Changes        │
         └──────────┬──────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ USER ACTION: Change Confidence Threshold    │
├─────────────────────────────────────────────┤
│ 1. User drags slider                        │
│    - Old value: 0.5                         │
│    - New value: 0.7                         │
│                                             │
│ 2. Update SettingsProvider                  │
│    settingsProvider.setConfidenceThreshold(0.7)│
│                                             │
│ 3. Save to SharedPreferences                │
│    prefs.setDouble('confidence_threshold', 0.7)│
│                                             │
│ 4. Apply Immediately                        │
│    - TFLiteService reads new value          │
│    - Future detections use 0.7 threshold    │
│                                             │
│ 5. Visual Feedback                          │
│    - Slider updates                         │
│    - Show value label: "70%"                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ USER ACTION: Toggle Sound Effects           │
├─────────────────────────────────────────────┤
│ 1. User taps switch                         │
│    - Current: ON (true)                     │
│    - New: OFF (false)                       │
│                                             │
│ 2. Update SettingsProvider                  │
│    settingsProvider.setSoundEnabled(false)  │
│                                             │
│ 3. Save to SharedPreferences                │
│    prefs.setBool('sound_enabled', false)    │
│                                             │
│ 4. Apply Immediately                        │
│    - No sound plays on next detection       │
│                                             │
│ 5. UI Updates                               │
│    - Switch animates to OFF position        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ USER ACTION: Clear History                  │
├─────────────────────────────────────────────┤
│ 1. User taps "Clear History" button         │
│                                             │
│ 2. Show confirmation dialog                 │
│    "Delete all detection history?"          │
│    [Cancel] [Clear]                         │
│                                             │
│ 3. If confirmed:                            │
│    DatabaseService.clearAllHistory()        │
│                                             │
│ 4. Show success message                     │
│    "✅ History cleared successfully"         │
└─────────────────────────────────────────────┘
```

---

## 🗄️ Database Operations Workflow

### **Insert History:**

```
┌─────────────────────────────────────────────┐
│ DatabaseService.insertHistory(history)      │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 1. Validate Input                           │
│    - Check detectedText not empty           │
│    - Verify timestamp format                │
│    - Ensure confidence 0-1 range            │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Get Database Instance                    │
│    final db = await database;               │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Execute SQL Insert                       │
│    await db.insert(                         │
│      'history',                             │
│      {                                      │
│        'detected_text': 'HELLO',            │
│        'timestamp': '2025-12-17 01:22:55',  │
│        'confidence': 0.87,                  │
│        'duration': 45                       │
│      },                                     │
│      conflictAlgorithm: ConflictAlgorithm   │
│        .replace                             │
│    );                                       │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Return Success/Failure                   │
│    - If success: return true                │
│    - If error: log error, return false      │
└─────────────────────────────────────────────┘
```

### **Query History:**

```
┌─────────────────────────────────────────────┐
│ DatabaseService.getAllHistory()             │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 1. Get Database Instance                    │
│    final db = await database;               │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Execute SQL Query                        │
│    final List<Map<String, dynamic>> maps =  │
│      await db.query(                        │
│        'history',                           │
│        orderBy: 'timestamp DESC',           │
│        limit: 50                            │
│      );                                     │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. Convert to Model Objects                 │
│    return List.generate(maps.length, (i) {  │
│      return GestureHistory.fromMap(maps[i]);│
│    });                                      │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Return List<GestureHistory>              │
│    Example result:                          │
│    [                                        │
│      GestureHistory(...),                   │
│      GestureHistory(...),                   │
│      ...                                    │
│    ]                                        │
└─────────────────────────────────────────────┘
```

---

## 🎯 State Management Flow (Riverpod)

### **How Riverpod Works in This App:**

```
┌─────────────────────────────────────────────┐
│ 1. Define Provider                          │
├─────────────────────────────────────────────┤
│ final gestureDetectionProvider =           │
│   StateNotifierProvider<                    │
│     GestureDetectionNotifier,               │
│     GestureDetectionState                   │
│   >((ref) {                                 │
│     return GestureDetectionNotifier();      │
│   });                                       │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Widget Watches Provider                  │
├─────────────────────────────────────────────┤
│ class CameraScreen extends ConsumerWidget { │
│   @override                                 │
│   Widget build(BuildContext context,        │
│                WidgetRef ref) {             │
│                                             │
│     // WATCH provider                       │
│     final state = ref.watch(                │
│       gestureDetectionProvider             │
│     );                                      │
│                                             │
│     // Use state data                       │
│     return Text(state.detectedText);        │
│   }                                         │
│ }                                           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. State Changes                            │
├─────────────────────────────────────────────┤
│ // User presses "Add Space" button          │
│ ref.read(gestureDetectionProvider.notifier) │
│    .addSpace();                             │
│                                             │
│ // Inside GestureDetectionNotifier:         │
│ void addSpace() {                           │
│   state = state.copyWith(                   │
│     detectedText: state.detectedText + ' '  │
│   );                                        │
│ }                                           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Automatic UI Rebuild                     │
├─────────────────────────────────────────────┤
│ - Riverpod detects state change             │
│ - Notifies all watchers                     │
│ - Widget rebuilds automatically             │
│ - UI shows new detectedText                 │
│                                             │
│ Old UI: "HELLO"                             │
│ New UI: "HELLO "                            │
└─────────────────────────────────────────────┘
```

---

## 📱 Permission Handling Workflow

```
┌─────────────────────────────────────────────┐
│ App Needs Camera Permission                 │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 1. Check Current Permission Status          │
│    final status = await Permission          │
│      .camera.status;                        │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────┬─────────────────┐
         ↓                 ↓                 ↓
    GRANTED          DENIED        PERMANENTLY DENIED
         |                |                  |
         v                v                  v
  ┌──────────┐   ┌──────────────┐   ┌──────────────┐
  │ Proceed  │   │ Request      │   │ Show Dialog  │
  │ with     │   │ Permission   │   │ "Please      │
  │ Camera   │   │              │   │ enable in    │
  │          │   │ await        │   │ Settings"    │
  └──────────┘   │ Permission   │   │              │
                 │ .camera      │   │ [Settings]   │
                 │ .request()   │   └──────────────┘
                 └──────┬───────┘           |
                        ↓                   v
              ┌────────────────┐    openAppSettings()
              │ User Response  │
              └────────┬───────┘
                       ↓
         ┌─────────────┴─────────────┐
         ↓                           ↓
    ALLOWED                     DENIED AGAIN
         |                           |
         v                           v
  ┌──────────┐              ┌────────────────┐
  │ Proceed  │              │ Show Error     │
  │ with     │              │ "Camera needed"│
  │ Camera   │              │ Disable feature│
  └──────────┘              └────────────────┘
```

---

## 🎨 UI Update Flow

```
Detection Happens
         |
         v
┌─────────────────────────────────────────────┐
│ TFLiteService Returns Prediction            │
│ PredictionResult(                           │
│   label: "A",                               │
│   confidence: 0.87                          │
│ )                                           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ GestureDetectionProvider Updates State      │
│ state = state.copyWith(                     │
│   currentGesture: "A",                      │
│   currentConfidence: 0.87,                  │
│   detectedText: detectedText + "A"          │
│ );                                          │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ Riverpod Notifies All Watchers              │
└──────────────────┬──────────────────────────┘
                   ↓
         ┌─────────────────┬─────────────────┐
         ↓                 ↓                 ↓
 PredictionOverlay  ConfidenceMeter  DetectedTextDisplay
         |                 |                 |
         v                 v                 v
    Rebuilds          Rebuilds          Rebuilds
    Shows "A"      Updates bar to 87%  Shows "...A"
    with fade      Animates color      Scrolls if
    animation      to green            needed
         |                 |                 |
         └─────────────────┴─────────────────┘
                          ↓
                ┌──────────────────┐
                │ Screen Updates   │
                │ Smoothly         │
                │ (60 FPS)         │
                └──────────────────┘
```

---

## 🔄 Complete App Data Flow

```
                    ┌─────────────┐
                    │   USER      │
                    └──────┬──────┘
                           │
                ┌──────────┼──────────┐
                ↓          ↓          ↓
        ┌─────────┐  ┌─────────┐  ┌──────────┐
        │ Camera  │  │ Forms   │  │ Gestures │
        │ Input   │  │ Input   │  │ Input    │
        └────┬────┘  └────┬────┘  └────┬─────┘
             │            │            │
             └────────────┼────────────┘
                          ↓
                  ┌───────────────┐
                  │  SERVICES     │
                  ├───────────────┤
                  │ TFLite        │
                  │ Camera        │
                  │ Database      │
                  │ TTS           │
                  └───────┬───────┘
                          ↓
                  ┌───────────────┐
                  │  PROVIDERS    │
                  │  (State Mgmt) │
                  └───────┬───────┘
                          ↓
                  ┌───────────────┐
                  │  WIDGETS      │
                  │  (UI Display) │
                  └───────┬───────┘
                          ↓
                    ┌─────────┐
                    │ SCREEN  │
                    │ Updates │
                    └─────────┘
```

---

## 🎯 Performance Optimization Workflow

### **Frame Processing Optimization:**

```
Problem: Too many frames → Slow app

Solution:
┌─────────────────────────────────────────────┐
│ 1. Frame Rate Limiting                      │
│    - Don't process every frame              │
│    - Skip frames if previous not done       │
│                                             │
│    bool isProcessing = false;               │
│                                             │
│    camera.startImageStream((image) {        │
│      if (isProcessing) return; // Skip      │
│      isProcessing = true;                   │
│                                             │
│      processFrame(image).then((_) {         │
│        isProcessing = false;                │
│      });                                    │
│    });                                      │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 2. Image Compression                        │
│    - Reduce quality before inference        │
│    - Use lower resolution                   │
│    - Trade-off: speed vs accuracy           │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 3. Isolate Heavy Computation                │
│    - Run inference in separate thread       │
│    - Don't block UI thread                  │
│    - Use Flutter Isolates                   │
│                                             │
│    await Isolate.spawn(                     │
│      runInference,                          │
│      image                                  │
│    );                                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 4. Widget Rebuild Optimization              │
│    - Only rebuild changed widgets           │
│    - Use const constructors                 │
│    - Separate stateful/stateless wisely     │
└─────────────────────────────────────────────┘
```

---

## 🔒 Error Handling Workflow

```
┌─────────────────────────────────────────────┐
│ Potential Error Points                      │
└─────────────────────────────────────────────┘
         |
         └─┬─────────┬─────────┬──────────┬────────────┐
           ↓         ↓         ↓          ↓            ↓
    ┌──────────┐ ┌───────┐ ┌──────┐ ┌─────────┐ ┌──────────┐
    │ Model    │ │Camera │ │ DB   │ │ Network │ │ Perms    │
    │ Loading  │ │ Init  │ │Query │ │ Request │ │ Denied   │
    └────┬─────┘ └───┬───┘ └──┬───┘ └────┬────┘ └────┬─────┘
         │           │        │          │           │
         ↓           ↓        ↓          ↓           ↓
    try-catch   try-catch  try-catch try-catch   Status check
         │           │        │          │           │
         ↓           ↓        ↓          ↓           ↓
    ┌─────────────────────────────────────────────────────┐
    │ Error Handling Strategy                             │
    ├─────────────────────────────────────────────────────┤
    │ 1. Log error (for debugging)                        │
    │ 2. Show user-friendly message                       │
    │ 3. Provide recovery action                          │
    │ 4. Fallback to safe state                           │
    └─────────────────────────────────────────────────────┘
```

**Example:**

```dart
try {
  await tfliteService.loadModel();
} catch (e) {
  // 1. Log
  print('Error loading model: $e');
  
  // 2. User message
  showDialog(
    context: context,
    builder: (context) => AlertDialog(
      title: Text('Model Error'),
      content: Text('Failed to load AI model. Please restart the app.'),
      actions: [
        // 3. Recovery action
        TextButton(
          onPressed: () => retryLoadModel(),
          child: Text('Retry'),
        ),
      ],
    ),
  );
  
  // 4. Fallback
  setState(() {
    modelLoaded = false;
    disableDetectionFeatures();
  });
}
```

---

## 📊 Summary: Complete Workflow Overview

```
┌──────────────┐
│ APP LAUNCH   │
└──────┬───────┘
       │
       ↓ (Initialize Everything)
       │
┌──────┴────────────────────────────────────────┐
│              HOME SCREEN                      │
│  ┌─────────────────────────────────────────┐ │
│  │ Navigation Hub + Statistics Display     │ │
│  └─────────────────────────────────────────┘ │
└─┬──────┬──────┬──────┬──────┬──────┬────────┘
  │      │      │      │      │      │
  ↓      ↓      ↓      ↓      ↓      ↓
Camera Learn Practice Reverse History Settings
  │      │       │       │       │       │
  ↓      ↓       ↓       ↓       ↓       ↓
Real-  Mark   Test    Text→   View   Configure
Time   As     Skills   Signs  Past    App
Detect Learned                Data
  │      │       │       │       │       │
  ↓      ↓       ↓       ↓       ↓       ↓
Save→Database←Progress←─────────┴───Settings
  │      │       │
  └──────┴───────┴──→ [SQLite + SharedPrefs]
                           │
                           ↓
                      Persistent
                      Storage
```

---

## 🎯 Key Takeaways for Viva

### **When explaining workflows, remember:**

1. **Start Simple:** User action → System response
2. **Show Data Flow:** Where data comes from and goes
3. **Mention Services:** Which service handles what
4. **State Management:** How UI stays in sync
5. **Error Handling:** What if something fails
6. **Performance:** Why you made certain choices

### **Practice explaining:**
- "When user opens camera, first we check permission, then initialize camera controller, then load model if needed, then start frame processing loop..."
- "Each frame goes through preprocessing: resize→normalize→inference→post-process→display"
- "State is managed by Riverpod providers which notify widgets when data changes, causing automatic UI rebuilds"

---

**You've got this! The workflow is logical and you built it yourself. Just walk through it confidently step by step.** 💪🚀
